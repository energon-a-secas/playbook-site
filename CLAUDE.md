# CLAUDE.md: Playbook

Playbook: bilingual career advice for tech workers, ES/EN with series navigation (playbook.neorgon.com)

**Live:** playbook.neorgon.com · **Port:** 8819

## Run

```bash
make serve
```

Then open http://localhost:8819. It must be served over HTTP. The app is ES modules, and `file://` blocks them.

## Architecture

| Module | Lines | Owns |
|---|---:|---|
| `js/post.js` | 285 | none |
| `js/reading.js` | 171 | `setDocumentLang`, `findSlugContext`, `renderBreadcrumbs`, `renderPostMeta`, `renderSeriesOutline` |
| `js/render.js` | 125 | `renderCards`, `renderHeroTopics` |
| `js/app.js` | 54 | none |
| `js/utils.js` | 41 | `slugify`, `escHtml`, `toast`, `readTime` |
| `js/events.js` | 27 | `setupLangToggle`, `updateLangButtons` |
| `js/state.js` | 22 | `state`, `loadSaved`, `save` |

Vendored from `packages/neorgon-ui/`: never edit in place, run the sync script instead: `js/neorgon-footer.js`, `js/neorgon-header.js`.

## Data

- `localStorage['playbook-lang']`
- `data/`: 1 file(s)

## Conventions

- Zero build step. Plain ES modules loaded by `js/app.js`.
- Header and footer come from the shared kits. Do not add site-local `.neo-footer` or `.header-bar` CSS.
- No single JS file over ~500 lines. It currently holds.
- `js/app.js` is 54 lines against a 50-line guideline; it should be an entry point, not a module.

## Gotchas

TODO: the non-obvious failures. What broke here before, what looks wrong but is deliberate, what a reasonable change would break. This is the highest-value section, leave it empty rather than filling it with generic advice.

## Do not touch

- `js/neorgon-*.js` and `css/neorgon-*.css`: vendored kits, regenerated by `packages/neorgon-ui/sync-*.sh`.
